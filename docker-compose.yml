version: "2"
services:
    app:
        container_name: restfulness
        build: ./
        links:
            - db
        ports:
            - "5000:5000"

    db:
        container_name: mysql
        image: mysql:8.0
        environment:
            MYSQL_ROOT_PASSWORD: root
        volumes:
            - ./:/docker-entrypoint-initdb.d/:ro